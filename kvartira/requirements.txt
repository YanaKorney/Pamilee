# Зависимости приложения «Моя квартира».
# Ставятся автоматически при первом запуске — вручную ничего делать не нужно.

fastapi==0.115.6          # веб-сервер (маршруты, проверка данных)
uvicorn==0.34.0           # запускает веб-сервер
python-multipart==0.0.20  # приём загружаемых файлов
pymupdf==1.25.2           # чтение PDF: страницы, текст, векторная геометрия
pillow==11.1.0            # работа с изображениями
httpx==0.28.1             # обращения к AI-сервисам
