# Обязательные библиотеки приложения «Моя квартира».
# Ставятся автоматически при первом запуске — вручную ничего делать не нужно.
#
# Версии указаны как «не ниже», а не «точно такая». Это важно: для самых
# свежих версий Python готовых сборок старых библиотек может ещё не быть,
# и установка падает. Так установщик сам подберёт подходящую версию.

# веб-сервер: маршруты и проверка данных
fastapi>=0.115
# запускает веб-сервер
uvicorn>=0.34
# приём загружаемых файлов
python-multipart>=0.0.20
# чтение PDF (страницы, текст, векторная геометрия), а также JPG и PNG
pymupdf>=1.25
# обращения к AI-сервису
httpx>=0.28
