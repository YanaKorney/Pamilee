# Батник Windows обязан иметь переводы строк CRLF: с LF командная оболочка
# спотыкается на метках goto. Фиксируем, чтобы git не переписал их обратно.
*.bat text eol=crlf

# Скрипт для macOS — наоборот, только LF.
*.command text eol=lf
*.sh      text eol=lf
